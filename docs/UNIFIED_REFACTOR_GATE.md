# Unified refactor gate

This temporary gate records the acceptance conditions for the consolidation
pull request.

- The active compiler code imports no superseded near-optimal or checkpoint
  resource module.
- The conditioned-prefix implementation uses the repository's self-contained
  binary-to-one-hot tree decoder.
- The complex phase layer is represented as one exact uniformly controlled
  one-qubit gate.
- Yuan and Zhang, *Quantum* 7, 956 (2023), are the sole active external compiler
  framework and optimal-QSP benchmark.
- Sun et al. are retained as historical predecessor and original-source
  attribution.
- The strict zero-workspace endpoint remains explicit.
- The deterministic suite and unified resource ledger pass on Python 3.11 and
  3.13.

Remove this temporary gate after the consolidation pull request has passed its
final proof and repository audit.
